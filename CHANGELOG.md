# Changelog

All notable changes to HF Model Downloader (egui) are documented here.

## v0.2.6 (2026-07-29)

UI 布局修复。

### Fixed
- **文件列表最底行被遮挡**：底部「保存到」栏原在 `CentralPanel` 之后显示，导致主内容区未为其预留空间、底部栏盖住文件列表最后一行。已将底部栏改在 `CentralPanel` 之前显示，文件列表区域整体下移，最后一行可完整显示且滚动到底不再被遮。

## v0.2.5 (2026-07-29)

UI 与下载行为修正。

### Changed
- **底部「保存到」改为只读显示**：显示实际保存目录（默认下载目录 + 仓库子目录，如 `默认目录/cyankiwi-Qwen3.6-27B-AWQ-BF16-INT4`），以红色 label 展示，不可编辑；默认下载目录改在设置对话框中「默认下载目录」选择。
- **仓库地址输入提示上移**：`输入仓库地址,如 owner/repo …` 移到标题「HF 模型下载器」之后作为淡色副标题，输入框区域更干净。
- **列出文件状态提示位置**：`已列出: N 个文件` 等信息移到「列出文件」按钮正下方一行。

### Fixed
- **单文件暂停不再影响其它文件**：之前点某个文件的「暂停」会经任务级取消标志把全部文件都暂停；现改为按文件暂停（仅该文件停止、断点续传，其余继续下载）。
- **再次「开始下载」时进度误显 0%**：之前 UI 把所有文件重置为 0% 且引擎对已完成的文件静默跳过、不回传状态；现引擎对已完成的文件回传真实状态，不再显示 0%。
- **再次「开始下载」状态卡在「正在开始下载…」**：全部完成再次开始时不再 spawn 下载线程、也不会发出完成信号，状态会一直停留「正在开始下载…」；现无任何文件需下载时主动发出完成信号，正确显示「下载完成」。
- **已完成文件与磁盘二次校验**：再次开始时核对磁盘，若文件被删除/截断则重新下载，避免内存里 Done 但磁盘缺失的误判。

## v0.2.4 (2026-07-29)

UI 细节继续打磨。

### Changed
- **「开始下载」按钮位置**：从底部「保存到」栏移到「全选 / 全不选」按钮之后，挑选完文件即可立即点击下载。
- **文件列表文件大小**：改为独立右对齐列（文件名 | 大小，两列表格形式），不再紧跟文件名。
- **列出文件状态提示**：`已列出: N 个文件` 等信息移到「列出文件」按钮正下方的一行，不再落在底部状态栏。
- **仓库地址输入提示**：`输入仓库地址,如 owner/repo …` 移到顶部标题「HF 模型下载器」之后作为淡色副标题，输入框区域更干净。
- **「保存到」标签对齐**：底部栏「保存到」标签相对目录输入框做了轻微下沉，与输入框、浏览按钮视觉更协调。

## v0.2.3 (2026-07-28)

UI 布局与交互优化。

### Changed
- **单列布局**：去掉左右分栏，改为从上到下单一纵向排列 —— 仓库地址输入框 → 列出文件 → 文件列表（含进度）→ 保存到 → 开始下载。
- **文件列表与进度合并**：每个文件一行显示「复选框 + 文件名」，其下直接显示进度条；文件名过长时自动换行、进度条落到文件名下方，不再横向溢出。
- **进度条显示预估完成时间（ETA）**：在下载速度之外，按平均速度估算剩余完成时间（`H:MM:SS` / `M:SS`）。
- **滚动条位置修正**：文件列表滚动区改为撑满整宽，竖向滚动条固定在最右侧（之前会缩到内容宽度、落在中间）。
- **输入框右键菜单**：所有文本框（仓库地址、保存到、设置、登录 Token）右键支持「剪切 / 复制 / 粘贴 / 全选」，不再只能靠 Ctrl+V。
- **按钮语义纠正**：下载中的「取消」实为**暂停**、「重试」实为**继续下载**，相应文案与状态（已暂停）已更正。
- **默认全不选**：列出文件后不再默认全选，由用户勾选需要下载的文件（仍保留「全选」按钮）。

## v0.2.2 (2026-07-28)

Download reliability and resume fixes.

### Fixed
- **断点续传在「取消后继续」时失效**：任务的 `cancelled` 标志只置位、从不复位，导致取消后再点「继续下载」会立刻被判定为已取消而退出，文件永远停在半途。现在 `start` / `retry` 会正确清零该标志，续传真正可用。
- **切换镜像站点不生效**：复用已有任务时忽略新的 endpoint / 下载目录，切到 `https://hf-mirror.com` 再继续仍走旧地址。现在 `get_or_create` 会同步最新 endpoint、下载目录与 revision。
- **「重试」按钮点击无反应**：`engine.retry` 先把文件状态设为 `Pending`，而 `start_downloads` 会跳过所有 `Pending` 文件，导致重试什么都不做。现在 `retry` 不再预置 `Pending`，交由 `start_downloads` 复位 `Failed/Cancelled` 并真正派发下载；同时 `retry` 会写入当前 endpoint / 下载目录，使「停止 → 切镜像 → 重试」真正走新镜像续传。

### Improved
- **下载速度更稳定**：单文件在遇瞬时网络错误 / 超时（5xx、429、连接错误）时自动重试最多 3 次（退避 1 / 2 / 5 秒），每次从磁盘已下载字节处续传，网络抖动不再直接整文件失败。

### Tests
- 新增本地自动化测试（手搓 HTTP/1.1 + Range 服务器）覆盖：整文件下载、取消后续传、镜像 endpoint 切换生效、瞬时错误自动重试、`retry` 重试取消文件并应用新 endpoint。

## v0.2.1 (2026-07-27)

- 所有输入框加黑边、输入内容自动裁剪前后空格。
- 设置页两个输入框宽度上限 420px。
- 新增 macOS CI，产出 Intel + Apple Silicon 通用二进制 tar 包。
- 跨平台 CJK 字体探测 + 内置 Noto Sans CJK 字体（Linux / macOS 自包含中文显示）。
- 使用 rustls-tls（纯 Rust TLS，零系统依赖）。

## v0.2.0 (2026-07-27)

- 从 Tauri v2 重写为 egui + eframe 原生桌面应用，单文件零依赖 exe。
- 静态链接 CRT（Windows 无需 VC++ 运行库）。
- 外置 `lang/*.json` 多语言，自动跟随系统语言，用户可自行翻译新增语言。
- 支持 HF Token 登录、镜像 / 加速节点、并行下载、取消与重试。

## v0.1.0 (2026-07-26)

- 初始版本：列文件、勾选下载、进度与速度显示。
